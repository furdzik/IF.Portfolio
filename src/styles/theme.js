import { colors, typography } from './basic';

export const theme = {
  ...colors,
  ...typography,
  layout: {
    width: '100rem',
    sidePadding: '2rem',
    background: '#95e6c3'
  }
};

export default theme;
